# Explore LB label verification

The Home hero CTA now reads “Explore LB” and still links to `/explore`. The Explore page heading now reads “Explore LB,” while its supporting sentence preserves the broader “across the Philippines” platform direction.

At 390px mobile, the CTA fits on one line and the new Featured Events & Destinations heading remains separate from the Explore CTA. At 1280px desktop, the hero action and Featured section heading remain aligned with the existing editorial layout. The Explore route and existing filters are unchanged.

Validation completed with 53 passing tests, TypeScript passing, and a successful production build. Existing warnings remain limited to the runtime `/manus-storage` reference and bundle-size advisory.
